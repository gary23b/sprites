package ebitensim

/*
import (
	"fmt"
	"testing"
)

func Test_llInsert(t *testing.T) {
	a := &ebitenSprite{z: 0}
	b := &ebitenSprite{z: 1}
	c := &ebitenSprite{z: 2}
	d := &ebitenSprite{z: 12}
	e := &ebitenSprite{z: -1}

	fmt.Println(a)
	fmt.Println(b)

	var head *ebitenSprite

	llInsert(&head, a)
	llInsert(&head, b)
	llInsert(&head, c)
	llInsert(&head, d)
	llInsert(&head, e)

	fmt.Println(a)
	fmt.Println(b)
}
*/
